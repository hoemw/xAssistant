[FilterPlugin]
; 列出需要一并打包的附加资源，路径相对于插件根目录（与 SmartUEAssistant.uplugin 同级）
; 支持通配：...（递归目录）、*（任意多字符）、?（单字符）
; 强制包含 Config 目录（含其所有层级）
WhitelistDirectories=Config
; 如需额外包含资源，可按需追加：
; WhitelistDirectories=Resources
; WhitelistDirectories=ThirdParty